import React from 'react'
import Navbar from './Navbar'

const Header = () => {
  return (
    <header className='flex items-center justify-center w-full bg-gray-950'>
        <Navbar/>
    </header>
  )
}

export default Header